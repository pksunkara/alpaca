{
  "name": "{{.Module.package}}",
  "version": "{{.Module.version}}",
  "description": "{{if .Module.official}}Official {{end}}{{.Module.name}} API client for node.js",
  "author": "{{.Module.author.name}} <{{.Module.author.email}}> ({{.Module.author.url}})",
  "homepage": "{{.Module.url}}",
  "repository": {
    "type": "git",
    "url": "https://{{.Module.git.site}}/{{.Module.git.user}}/{{.Module.git.name}}-node"
  },
  "main": "./lib/index.js",
  "dependencies": {
    "request": "2.27.x"
  },
  "bugs": {
    "url": "https://{{.Module.git.site}}/{{.Module.git.user}}/{{.Module.git.name}}-node/issues"
  },
  "license": "{{.Module.license}}",
  "engines": {
    "node": ">= 0.8.0"
  }
}
